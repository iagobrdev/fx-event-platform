server:
  port: 8080

spring:
  application:
    name: api-gateway
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/fx}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when_authorized
